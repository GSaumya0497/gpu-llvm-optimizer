# gpu-llvm-optimizer
A compiler research framework that analyzes and optimizes GPU kernels via LLVM IR, featuring CFG analysis, memory access profiling, and custom optimization passes for performance tuning.
